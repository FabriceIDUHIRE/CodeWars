function addBinary(a, b) {
  // Add the two numbers
  const sum = a + b;

  // Convert the sum to binary and return as a string
  return sum.toString(2);
}


addBinary(1, 1);     
addBinary(5, 9);     
addBinary(0, 0);     
addBinary(10, 10);   
